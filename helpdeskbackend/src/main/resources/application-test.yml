---
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;INIT=CREATE SCHEMA IF NOT EXISTS profile
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
#    generate-ddl: true
#    hibernate.ddl-auto: create-drop
#    show-sql: true